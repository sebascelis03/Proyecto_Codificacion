# Implementation Tasks (POS Frontend)

- [x] 1. Set up Next.js project with TypeScript and Tailwind CSS.
- [x] 2. Define TypeScript interfaces for `Product`, `CartItem`, and `Sale` in the core layer.
- [x] 3. Configure Zustand stores (`inventoryStore` and `cartStore`) as adapters.
- [x] 4. Build `ProductSearch` component for fuzzy text search and barcode scanning.
- [x] 5. Build `ProductGrid` component ensuring it does NOT show products until a search is performed.
- [x] 6. Build `CartPanel` to display items and real-time totals.
- [x] 7. Build `PaymentModal` to handle cash payments and change calculation.
- [x] 8. Assemble components into the main POS layout.
- [x] 9. Implement the checkout logic to clear the cart and show receipt.
- [x] 10. Remove Admin Dashboard completely to keep focus on POS Terminal.
- [x] 11. Refactor directory structure to strict Hexagonal Architecture (`core`, `features`, `adapters`).
